{{#if isValid}}
<div>
    {{#each docs}}
    <div>
        <h2>Producto: {{this.title}} &nbsp; descripcion: {{this.description}}</h2>
        <h4>Precio: {{this.price}} &nbsp; Categoria: {{this.category}} &nbsp; Stock: {{this.stock}}</h4>
    </div>
    <br>
    {{/each}}
</div>
<br>
<div>
    {{#if hasPrevPage}}
    <a href={{this.prevLink}}><< Anterior</a>
    {{/if}}
    &nbsp;
    {{this.page}}
    &nbsp;
    {{#if hasNextPage}}
        <a href={{this.nextLink}}>Siguiente >></a>
    {{/if}} 
</div>
{{else}}
<h1>No hay productos disponibles</h1>
{{/if}}